-- neotest adapters for the Personal profile (Ruby / Go).
-- The Go adapter comes from LazyVim's `lang.go` extra, so only the Ruby one
-- needs declaring here.
return {
	{ repo = "zidhuss/neotest-minitest", adapter = "neotest-minitest" },
}
